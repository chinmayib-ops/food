/* ============================================================
   Bengaluru Eats — backend config
   ------------------------------------------------------------
   Paste your Supabase project's URL and ANON (public) key below.
   Find them in: Supabase dashboard → Project Settings → API.

   The anon key is SAFE to commit publicly — your data is
   protected by the Row Level Security policies in schema.sql.

   Leave these as empty strings to run the app in offline,
   localStorage-only mode (no accounts, no sync).
   ============================================================ */
window.BE_CONFIG = {
  SUPABASE_URL: "",
  SUPABASE_ANON_KEY: ""
};
